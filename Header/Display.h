#include <string.h>
#include <iostream>
#include "general.h"
using namespace std;


class Display {
	public:
        int accum;
        ;
    public:
    	Display(string init);
    	~Display();
    private:
        void coinInput();
        
};